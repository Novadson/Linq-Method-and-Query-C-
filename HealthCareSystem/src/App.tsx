import Button from "./components/Button";

function App() {
  return (
    <div>
      <Button onClick={() => alert("dsdsdsdsdsa")} color="primary">
        My Button
      </Button>
    </div>
  );
}

export default App;
